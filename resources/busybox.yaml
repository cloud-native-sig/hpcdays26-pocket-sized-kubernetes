apiVersion: v1
kind: Pod
metadata:
  name: toolbox
  namespace: ngnix-demo

spec:
  containers:
  - name: toolbox
    image: busybox
    imagePullPolicy: IfNotPresent

    command:
    - sleep
    - "3600"
